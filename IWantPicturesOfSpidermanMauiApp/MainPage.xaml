<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:IWantPicturesOfSpidermanMauiApp.DTO"
             x:Class="IWantPicturesOfSpidermanMauiApp.MainPage"
             Title="Pictures Of Spiderman">
    <StackLayout>
        <ImageButton Source="iwantpicturesofspiderman.jpg"
             Clicked="OnRefreshClicked"
             HeightRequest="200"
             WidthRequest="200" />
        <ListView ItemsSource="{Binding Pictures}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:PictureDTO">
                    <ViewCell>
                        <Grid Padding="10" Background="AliceBlue" Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.RowSpan="2"
                           Source="{Binding FileLocation}"
                           Aspect="AspectFit"
                           HeightRequest="100"
                           WidthRequest="100" />
                            <Label Grid.Column="1"
                           Text="{Binding Name}"
                           FontAttributes="Bold" />
                            <Label Grid.Row="1"
                           Grid.Column="1"
                           Text="{Binding Description}"
                           FontAttributes="Italic"
                           VerticalOptions="End" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
